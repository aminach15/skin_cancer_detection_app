import 'package:flutter/material.dart';
class MyBlog2 extends StatelessWidget {
  const MyBlog2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Blog 2'),
      ),
    );
  }
}