import 'package:flutter/material.dart';
class MyBlog1 extends StatelessWidget {
  const MyBlog1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Blog 1'),
      ),
    );
  }
}
