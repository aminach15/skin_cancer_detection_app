import 'package:flutter/material.dart';
class MyBlog4 extends StatelessWidget {
  const MyBlog4({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Blog 4'),
      ),
    );
  }
}