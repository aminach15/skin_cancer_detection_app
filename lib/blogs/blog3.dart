import 'package:flutter/material.dart';
class MyBlog3 extends StatelessWidget {
  const MyBlog3({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Blog 3'),
      ),
    );
  }
}